<div class="individual-content grid gap-x-3	grid-cols-12">
  <div class="hero-card bg-white rounded col-span-12 px-5 py-4">
    <p class="text-sm text-gray-400 mb-2">Bem-vindo às suas finanças:</p>
    <h1 class="font-black text-4xl text-gray-700 mb-3">
      <%= @current_user.name %> 👋
    </h1>
    <p class="text-base font-light">
      Today <%= Time.now.strftime("%B %d, %Y") %>
    </p>
  </div>

  <% if @family_groups.any? %>
    <div class="family-groups">
      <% @family_groups.each do |family_group| %>
        <%= render FamilyGroupCardComponent.new(family_group: family_group) %>
      <% end %>
    </div>
  <% else %>
    <p>Você ainda não faz parte de nenhum Family Group.</p>
    <%= link_to "Criar novo Family Group", new_family_group_path, class: "btn btn-primary" %>
  <% end %>
</div>
